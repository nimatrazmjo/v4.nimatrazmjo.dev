export const AUTHORIZED_EMAIL = "nimatullah.razmjo@gmail.com";
export const PER_PAGE = 10;

/**
 * Professional identity, mirroring the resume headline. Single source for
 * page metadata, JSON-LD and on-page copy — change it here, not per section.
 */
export const ROLE = "Senior Software Engineer";
export const YEARS_EXPERIENCE = "12+";

/**
 * Availability. Consulting engagements only — not full-time roles — so the
 * hero badge and contact copy must not read as open-to-work.
 */
export const AVAILABILITY = "Available for consulting";

/** Capability chips: hero band, newsletter band, and metadata keywords. */
export const CAPABILITIES = [
  "Full Stack & Backend",
  "Python & FastAPI",
  "React & Next.js",
  "AWS & Terraform",
];
